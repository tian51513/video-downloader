import type { ReactNode } from "react"

function Popup() {
  return (
    <div
      style={{
        width: "400px",
        minHeight: "300px",
        padding: "16px",
      }}
    >
      <h2>Video Downloader</h2>
      <p>Extension loaded. Popup will be built in subsequent tasks.</p>
    </div>
  )
}

export default Popup
